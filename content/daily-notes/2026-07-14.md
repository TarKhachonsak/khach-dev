# 2026-07-14

## เป้าหมาย/งาน

เริ่มจากคำถามอธิบายโค้ดใน `crime-scene-location-redesign` (input `deferMapUntilRequested`, `forceShowCoordinatePicker`, getter `hasCoordinates`) แล้วขยายไปสู่การ debug และแก้บั๊กแผนที่ (Leaflet) ไม่แสดงผลในหลายจุดของฟอร์ม officer-task

## ลำดับปัญหาและวิธีแก้

1. **ปุ่ม "ระบุพิกัด" ไม่แสดงใน task flow หนึ่ง** — root cause คือ template bind property ชื่อผิด case (`[taskformcode]` แทน `[taskFormCode]`) และไม่ได้ bind `[forceShowCoordinatePicker]` เลย ทำให้ getter คืนค่า false เมื่อไม่มีพิกัดอยู่ก่อน

2. **User แก้เอง (นอกเซสชัน)**: ลบ `deferMapUntilRequested`, `forceShowCoordinatePicker`, `hasCoordinates` ออกจาก component ทั้งหมด (ยืนยันว่าตั้งใจ) — ทำให้ context ของปัญหาก่อนหน้าเปลี่ยนไป ต้อง sync ใหม่ก่อนแก้ต่อ

3. **แผนที่ไม่แสดงหลังเปิด collapsible section** — root cause: Leaflet map ถูก initialize ตอน container ยัง `display:none` (section ปิดอยู่) ทำให้คำนวณขนาดผิด และไม่มีจุดไหนเรียก `invalidateSize()` หลังเปิด section
   - Fix: เพิ่ม `invalidateSize()` ใน leaflet wrapper component, ลบการเรียก `mapComponent?.showMap()` ซ้ำใน `ngOnChanges` ของ parent (ตัวการที่ทำให้แผนที่ toggle ปิด/เปิดโดยไม่ตั้งใจ), เพิ่ม toggle handler เรียก `invalidateSize()` หลังเปิด section

4. **แผนที่ยัง blank ตอน auto-show ครั้งแรก (ไม่เกี่ยวกับ collapsible)** — เดาเป็น DevExtreme layout timing ก่อน (เพิ่ม delayed `invalidateSize()`) แต่ไม่ตรงจุด

5. **Root cause แท้จริง** (ยืนยันจาก browser console): `NG0100 ExpressionChangedAfterItHasBeenCheckedError` + `Uncaught Error: Map container not found` — component toggle `isMapVisible` ของตัวเองข้างใน `ngAfterViewInit` (หลัง view ถูก check ไปแล้ว) และ template ผูก `*ngIf="isMapVisible"` กับ div ที่เป็น container ของ Leaflet เอง — เกิด race: `L.map('map')` (lookup ด้วย string id) รันก่อนที่ Angular จะ re-render div เข้า DOM จริง
   - Fix รอบแรก: ลบ internal toggle + `*ngIf` ออกจาก template ให้ div render เสมอ, ทำ `showMap()` เป็น idempotent
   - ยังพังอยู่ (error เดิม) เพราะยังใช้ string id lookup ซึ่งไม่การันตี timing
   - Fix จริง: เปลี่ยนไปใช้ `@ViewChild('mapContainer', { static: true }) mapContainerRef: ElementRef` แล้วส่ง native element เข้า `L.map()` ตรงๆ แทน string id — `static: true` การันตีว่า element พร้อมตั้งแต่ก่อน `ngOnInit`

6. Build ผ่าน 0 Error ทุกรอบ — commit แยก 2 กลุ่มตามงาน (refactor crime-scene-location, fix leaflet-map-overview)

## Follow-up ที่ยังไม่ปิด (สำคัญ)

Leaflet wrapper component เป็น **shared component** ที่ใช้ร่วมกันในฟอร์มอื่นอีกอย่างน้อย ~25 ไฟล์ ทุกไฟล์มี pattern เดียวกัน:

```ts
onToggleMap(mapComponent: any) {
    this._isMapVisible = !this._isMapVisible;
    mapComponent.showMap();   // เดิมพึ่งพา toggle behavior
}
```

การเปลี่ยน `showMap()` จาก toggle (สร้าง/ทำลายสลับกัน) เป็น idempotent เพื่อแก้ race condition ทำให้ **ทุกฟอร์มที่ใช้ pattern นี้เสียความสามารถ "กดซ้ำเพื่อซ่อนแผนที่"** ไป — แจ้ง user แล้ว รอ decision ว่าจะจำกัดขอบเขตแค่ component เดียว หรือทำ fix แบบ backward-compatible (ซ่อน/แสดงด้วย CSS + `invalidateSize()` แทนการทำลาย/สร้างใหม่ทุกครั้ง) ให้ครอบคลุมทุก consumer ยังไม่ได้ implement — เป็นงานค้าง

## เชื่อมโยง

- [[Leaflet Map Zoom Reset on Position Change]]
- [[Angular Lifecycle Hooks — @ViewChild Timing]]
- [[Case-Mismatched Property Binding Silently No-ops]]
- [[Check Recent Commits Before Fixing Shared Component]]

---

## Session 2: PERSON_REPORT ใน PHINAI4 (bma-rod-officer-api)

### เป้าหมาย/งาน

เพิ่มการบันทึกข้อมูล PERSON_REPORT (ผู้แจ้ง/ผู้รายงาน) ให้ครบใน flow ของฟอร์ม PHINAI4
(`ROD_FIR_TXN_PERSON_REPORT`) ทั้งฝั่งเรียกใช้งานและฝั่ง field mapping

### ลำดับปัญหาและวิธีแก้

1. **`UpdateFormDraftAsync` ใน `Phinai4Service.cs` ไม่เคยเรียก `ReplacePersonReportAsync`**
   — เมธอด helper มีอยู่แล้วใน `FormProcessService.cs` (pattern replace-all: mark
   record เก่าเป็น `RECORD_STATUS = "I"` แล้ว insert ใหม่ทั้งหมด) แต่ไม่มีจุดไหนเรียกใช้ใน
   flow ของ `updateFormDraft` — แก้โดยเพิ่ม checkpoint `9B_PERSON_REPORT` เรียก
   `ReplacePersonReportAsync` ก่อน commit (เฉพาะจุด `updateFormDraft` ตามที่ user เลือก
   ไม่แตะ pattern เดียวกันที่ซ้ำอยู่ใน `createFormDraft`)

2. **`ReplacePersonReportAsync` map field ไม่ครบตาม entity จริง** — entity
   `ROD_FIR_TXN_PERSON_REPORT` มี property `PERSON_REPORT_RACE_ID`,
   `PERSON_REPORT_BIRTHDAY`, `PERSON_REPORT_AGE`, `PERSON_REPORT_FAX`,
   `PERSON_REPORT_OCCUPATION_ID`, `CONTACT_FLAG` อยู่แล้วแต่ยังไม่ได้ map — แก้โดย grep
   หา property จริงใน Designer.cs ก่อนเพิ่ม assignment ให้ครบตาม type

### Build Verification

`dotnet build` compile ผ่าน 0 CS error ทุกครั้งก่อนเสนอ commit message — พบ error
MSB3027/MSB3021 (copy step) เพราะ Visual Studio กำลัง debug `Officer.Api.exe` อยู่
ล็อกไฟล์ output ไม่ใช่ error จากโค้ด

### Commits

- `1e537fd` feat: เพิ่มบันทึก PERSON_REPORT ใน updateFormDraft ของ PHINAI4
- `5f1c9ad` feat: เพิ่ม mapping ฟิลด์ PERSON_REPORT ใน ReplacePersonReportAsync
- `a16dc4a` feat: เพิ่ม mapping ฟิลด์ PERSON_REPORT(CONTACT_FLAG) ใน ReplacePersonReportAsync

### Follow-up ที่ยังไม่ปิด

ยังไม่ได้เพิ่ม `ReplacePersonReportAsync` ในจุด `createFormDraft` (~บรรทัด 553 ของ
`Phinai4Service.cs`) — ถ้าต้องการให้ flow สร้างใหม่บันทึก PERSON_REPORT ด้วยเหมือนกัน
ต้องเพิ่มเติมภายหลัง

### เชื่อมโยง

- [[XPO Detail Records - Replace-All Sync Pattern]]
- [[Unmapped Entity Properties After Codegen]]
- [[ORM Child Record Sync - Replace-All vs Diff]]

---

## Session 3: NullReferenceException ใน StatusResult.Error + PHINAI10 sync bug (bma-rod-officer-api)

### เป้าหมาย/งาน

User ถามว่า error `NullReferenceException` จาก production log (stack trace ชี้ไปที่
`StatusResult.Error[T]` ที่เรียกจาก `Phinai7Service.CreateInvoiceAsync`) คืออะไร

### Root Cause

`StatusResult.cs` มี overload `Error<T>(string, T value)` กับ `Error<T>(string, Exception ex)`
— เมื่อเรียก `Error<object>("msg", null)` C# overload resolution เลือก `Exception`
overload เสมอ (เพราะเป็น type เจาะจงกว่า `object`) แล้วเมธอดอ่าน `ex.Message` โดยไม่เช็ค
null → NullReferenceException ทับ validation message ที่ตั้งใจไว้ ยืนยันด้วยการรัน
test program แยกจริง ไม่ใช่เดาจาก stack trace — พบ pattern นี้ซ้ำ 105 จุดทั่วโปรเจกต์
ยังไม่ได้แก้ (เสนอ fix แล้ว รอ user ยืนยัน)

### พบเพิ่มจาก git log (คนละ session/tab วันเดียวกัน)

Commit `f479b02`: PHINAI10 `UpdateTask` ไม่เคยเรียก `ReplaceOfficerFormGroupAsync` —
root cause แบบเดียวกับ PERSON_REPORT ใน Session 2 (shared helper มีอยู่แล้วแต่ไม่ถูกเรียก
ในบาง flow) เกิดซ้ำเป็นครั้งที่ 2 ในวันเดียวกัน

### เชื่อมโยง

- [[C# Overload Resolution - Null Argument Prefers More Derived Reference Type]]
- [[StatusResult.Error Overload Silently Swallows Null Exception]]
- [[Shared Sync Helper Not Wired Into Every Form Controller]]
- [[Overload Resolution With Null Argument]]
