# 2026-07-07

อ้างอิงจาก commit ที่ authored โดย TarKhachonsak ในวันนี้ (branch `dev-tar-redesign`)

## เป้าหมาย/งานวันนี้

แก้บั๊กหลายจุดใน BMA ROD Officer UI: ปุ่มระบุพิกัดหายในบาง flow_task, ฟิลด์ RESULT_EXPLAIN_FLAG ไม่ถูกบันทึกลง DB, validate ของ flow_04_task_006 ที่ขาดไป, เงื่อนไขแสดงฟิลด์ยื่นบันทึกคำชี้แจง, และเปิดให้เลือกเชื้อชาติที่ถูกล็อกไว้ผิด

## งานที่ทำ (เรียงตามเวลา commit)

### 1. `5e7bb26f` (10:40) — แสดงปุ่มระบุพิกัดในทุก flow_task

- ไล่หา flow_task ทั้งหมดที่เรียกใช้ `app-crime-scene-location-redesign` (5 host component: form-02, form-61, form-63, form-104, form-204) แล้ว cross-reference กับ `form-render.component.ts` เพื่อ list ทุก flow code ที่ผ่าน component นี้
- Root cause: `showCoordinatePicker` getter เดิม gate ด้วย `forceShowCoordinatePicker` (มีแค่ form-104 bind เข้ามา) หรือมี lat/lng อยู่แล้วเท่านั้น — flow อื่นที่ยังไม่เคยมีพิกัดมาก่อนเลยไม่มีทางเห็นปุ่ม
- แก้โดยให้ getter `return true` เสมอ (ตัดสินใจไม่ทำ allow-list เพราะทุก flow ต้องการเหมือนกันหมด)
- **Follow-up**: 2026-07-10 เพื่อนร่วมทีมแก้ไฟล์เดียวกันทับด้วย allow-list เฉพาะ flow code แทน — งานวันนี้ถูก override ไปแล้วในโค้ดปัจจุบัน ต้อง sync แนวทางกับทีม

### 2. `3db9b48c` (17:29) — RESULT_EXPLAIN_FLAG ไม่ลง DB / เปลี่ยน service เป็นพินัย 5

- อาการ: ส่งค่า `RESULT_EXPLAIN_FLAG` ออกไปได้แต่ไม่ถูกบันทึกจริง (ดึงไปออก report ไม่ได้)
- Root cause จริง: save ผ่าน `formInspectionLocation6Service.Updateform()` ซึ่งไม่ map field นี้เข้า DB/report ที่ถูกต้อง
- Fix: เปลี่ยนทั้ง Get/Save จาก `FormInspectionLocaltion6Service` เป็น `FormInspectionLocaltion5Service` (`.Get()`/`.Put()`) พร้อมเพิ่ม `RESULT_EXPLAIN_FLAG: this.approveValue` ในทั้ง 3 จุดที่ build payload
- Insight: `this.approveValue` (radio A/B/C/D) ถูกใช้ set ทั้ง `FACT_FINDING_FLAG` และ `RESULT_EXPLAIN_FLAG` พร้อมกัน — ไม่มี field ไหนผูกกับ `RESULT_EXPLAIN_FLAG` โดยตรง

### 3. `ad810e30` + `a81160fc` (15:28–15:30) — validate + field วันที่ยื่นบันทึกคำชี้แจง FLOW_04_TASK_006

- เพิ่ม case `FLOW_04_TASK_006` ใน `validateAllFormBetweenFlow()`: ถ้า `formValue === "B"` ต้องมี `PHINAI5_SUM_DATE` ไม่งั้น validate fail
- เพิ่ม field `PHINAI5_SUM_DATE` ให้แสดงเมื่อ `approveValue === 'B'` เท่านั้น — เชื่อมโยงกับบั๊กเก่า [[Approve Value Scoped to Wrong Task]]

### 4. `d86b4778` (16:07) — เปิดให้เลือกเชื้อชาติ

- ลบ `readOnly: true` ที่ล็อก dropdown เชื้อชาติไว้โดยไม่ตั้งใจ

## Decision ที่ตัดสินใจ

- `showCoordinatePicker`: เลือก "return true เสมอ" — ภายหลังถูก teammate override เป็น allow-list ต้อง reconcile
- `RESULT_EXPLAIN_FLAG`: root cause คือ service/endpoint ผิด ไม่ใช่แค่ payload ขาด field

## Open Questions / Follow-up

1. Sync กับทีมเรื่องแนวทาง `showCoordinatePicker` (return true vs allow-list)
2. ตรวจว่าฟิลด์อื่นที่เคยพึ่งพา service6 ยังทำงานถูกต้องหลังเปลี่ยนเป็น service5
3. บทเรียน: เช็ค `git log` ของ shared component ก่อนแก้ทุกครั้ง กันงานถูก override ทับ

## เชื่อมโยง

- [[Force-Show Feature by Task Code Allowlist]]
- [[Service Endpoint Mismatch Blocks Field From Persisting]]
- [[Check Recent Commits Before Fixing Shared Component]]
- [[Approve Value Scoped to Wrong Task]]
- [[Cross-Task Decision via Dynamic Flow Key]]
