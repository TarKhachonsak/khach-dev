# 2026-07-01

## งานที่ทำ

### fix: message error (form-38)
แก้ typo ใน error message ของ `onCalSchedule`  
`"เกิดข้อในการคำนวณค่าปรับ"` → `"เกิดข้อพลาดในการคำนวณค่าปรับ"`

### refactor: config array pattern สำหรับซ่อน radio option (form-34-flow4)
เดิม: inline `if` ใน subscribe callback  
ใหม่: `_applyFormioDisableRules()` loop ผ่าน config array `rules`  
เหตุผล: รองรับ flow อื่น ๆ ในอนาคตโดยเพิ่มแค่ 1 บรรทัดใน array

## สิ่งที่เรียนรู้

- **Recursive function** = function ที่เรียกตัวเอง ใช้ traverse nested tree เช่น formio schema
- `patch` เป็น local function ภายใน `disableFormioRadioOption` — ไม่ใช่ method ของ class
- ถ้า component ไม่เข้าเงื่อนไขใด → `return c` คืนค่าเดิมโดยไม่เปลี่ยน
- Config array pattern เหมาะเมื่อมี logic เดิมซ้ำกันหลายครั้งต่างกันแค่ data

## Commits

- `13b7aeef` fix: message error
- `450c8592` feat: refactor การซ่อน radio option ให้รองรับหลาย flow ด้วย config array

## เชื่อมโยง

- [[Patch Formio Schema Dynamically]]
