# 2026-07-08

อ้างอิงจาก commit ที่ authored โดย TarKhachonsak ในวันนี้ (branch `dev-tar-redesign`) — สร้างย้อนหลังจาก git log/diff เนื่องจากไม่มี session summary ของวันนี้ (session ที่สร้างโน้ตนี้เกิดขึ้นวันที่ 2026-07-12)

## เป้าหมาย/งานวันนี้

แก้บั๊ก/เพิ่มฟีเจอร์ย่อยหลายจุดใน BMA ROD Officer UI: เปิดใช้งานปุ่มบันทึกร่าง (form-51), แก้ data binding ของฟอร์มผู้กล่าวหา (form-58), ปรับเงื่อนไขแสดงปุ่มระบุพิกัด (form-61), เพิ่มฟิลด์ `RESULT_EXPLAIN_FLAG` ที่ขาดหายในสองฟอร์ม, จัดรูปแบบไฟล์ form-evidence-object, และแก้ style/behavior ของ date picker คืนวัตถุพยาน

## งานที่ทำ (เรียงตามเวลา commit)

### 1. `70c51c95` (10:08) — เพิ่ม `onClickSaveDraft` จริงใน form-51

เดิมเป็นแค่ popup "Coming Soon!" แก้เป็น async function ตาม pattern: capture signature → `_buildParams()` → `onSaveDraft(params)` → `DialogWarning`/`DialogSuccess` พร้อม `_isLoading` guard — เป็น pattern ย่อยของ [[Multi-Page Form Stepper Pattern]] ที่ไม่มี `ConfirmSave()`/validate เหมือน pattern เต็มของ form-131

### 2. `e35ec69c` (10:44) — bind data ผิดตัวแปรใน form-58 (`app-accuser-form-view`)

`[formData]` ไม่เคยถูก bind เข้า `app-accuser-form-view` เลย → `TypeError: formData is undefined` เพราะ `@Input() formData: any;` ไม่มี default แล้ว template ใช้ `formData.CONTACT_FLAG` ตรงๆ โดยไม่มี `?.` แก้โดย bind `[formData]="selectedAccuser"` ให้ตรงกับตัวแปรที่ `onEditAccuser()` เซ็ตค่าจริง — ดู root cause เต็มใน [[Missing Input Binding Causes Undefined Crash]]

### 3. `d0836ae3` (11:28) — เปลี่ยน `showCoordinatePicker` จาก unconditional true → allowlist ตาม `taskFormCode`

Override การตัดสินใจของตัวเองเมื่อวาน (2026-07-07) ที่เขียน `return true` เสมอ กลับมาเป็น allowlist 6 flow codes — รายละเอียดและบทเรียนเรื่อง requirement confirmation ดู [[Force-Show Feature by Task Code Allowlist]]

### 4–6. `75d25458` (11:29) + `6acf0276` (16:32) — เพิ่ม `RESULT_EXPLAIN_FLAG` ที่ขาดใน `_buildParams()` ของ form-61 และ form-58

ฟิลด์เดียวกับที่เจอปัญหา endpoint ผิดใน [[Service Endpoint Mismatch Blocks Field From Persisting]] เมื่อ 2026-07-07 แต่รอบนี้สาเหตุคือ FE ไม่ได้ spread field เข้า payload ตั้งแต่แรก (debug layer ที่ 1 ตาม debug order ในโน้ตนั้น) เกิดซ้ำ 2 ฟอร์มเพราะไม่มี shared base ที่ inject field นี้ให้อัตโนมัติ — ต้องเพิ่มเองทุกฟอร์มที่มี `_buildParams()` ของตัวเอง

### 7. `33af0f08` (13:40) — format line `form-evidence-object.component.html`

Re-indent ทั้งไฟล์ ไม่มี logic เปลี่ยน

### 8. `b3a603b6` (16:34) — แก้ grid วันที่คืนวัตถุพยาน (`form-evidence-object`)

เปิด `[enableEvidenceOutDate]="true"` และเพิ่ม `[allowEditing]="false"` ให้ column `EVIDENCE_OBJECT_OUT_DATE` ที่พื้นหลังเทาผิดที่เดียวเพราะขาด flag นี้ (`allowUpdating` ระดับ grid ไม่ครอบคลุม `allowEditing` ระดับ column) — root cause เต็มใน [[DevExtreme Column allowEditing vs Grid allowUpdating]] เปลี่ยน `readOnly` ของ date editor จากผูก input prop เดิมมาผูกกับ `isEditingObject` (state) แทน

### 9. `359e6b94` (16:43) — ปรับ debug flag ใน `environment.staging.ts`

Config เฉพาะ environment ไม่บันทึกค่าจริงตาม security rule

## Decision ที่ตัดสินใจ

- `showCoordinatePicker`: กลับไปใช้ allowlist ตาม flow code (ครั้งที่ 2 ของ decision นี้ ภายใน 2 วัน)
- `EVIDENCE_OBJECT_OUT_DATE` readonly ผูกกับ state (`isEditingObject`) แทน input prop

## Open Questions / Follow-up

1. ตรวจสอบ flow code อื่นที่อาจต้องการเห็นปุ่มระบุพิกัดแต่ตกหล่นจาก `FORCE_SHOW_COORDINATE_FLOW_CODES`
2. พิจารณา shared helper สำหรับ field ที่ต้อง spread ซ้ำทุกฟอร์ม (เช่น `RESULT_EXPLAIN_FLAG`) ลดบั๊กแบบ "ลืมเพิ่มฟิลด์" ที่เกิดซ้ำ
3. ~~แก้ daily note ของ 2026-07-07 ที่ระบุ authorship/วันที่ผิด~~ → แก้แล้ววันนี้ (ดู [[2026-07-07]])

## เชื่อมโยง

- [[Missing Input Binding Causes Undefined Crash]]
- [[DevExtreme Column allowEditing vs Grid allowUpdating]]
- [[Force-Show Feature by Task Code Allowlist]]
- [[Service Endpoint Mismatch Blocks Field From Persisting]]
- [[Multi-Page Form Stepper Pattern]]
- [[Angular Input Object Reference]]
